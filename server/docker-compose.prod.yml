version: '3'

services:

  sanjeev-express:
    build: 
      context: .
      args: 
        NODE_ENV: production
    ports:
      - "3001:3001"
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - NODE_ENV=production
    command: node server.js
    # env_file:
    #   - ./.env